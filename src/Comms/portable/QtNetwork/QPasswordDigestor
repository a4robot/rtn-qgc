#pragma once

// Include-path-shadowing redirector: with QGC_ENABLE_QT_NETWORK=OFF, this directory is
// prepended to the include path (src/Comms/CMakeLists.txt) instead of Qt6::Network being
// linked, so consumers' unqualified #include <QtNetwork/QPasswordDigestor> resolves here instead of to a
// Qt module header. See ../../QGCNetworkCompat.h for the actual class(es).
#include "../QGCNetworkCompat.h"
